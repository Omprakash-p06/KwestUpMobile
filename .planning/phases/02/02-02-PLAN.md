---
phase: 02-tasks
plan: 02
type: execute
wave: 2
depends_on: [02-01-PLAN.md]
files_modified: [TaskListScreen.js, TaskCard.js, TaskEditModal.js]
autonomous: true
requirements: [TASK-03]
must_haves:
  truths:
    - "Task lists render inside separate sheets snap-paging horizontally"
    - "Task progress displays checklist fraction (completed/total) and progress bar"
    - "Subtasks can be completed inline with dynamic parent updates"
    - "Tasks can be relocated to a new category list inside the meta editor"
  artifacts:
    - path: "src/screens/TaskListScreen.js"
      provides: "ViewPager horizontal paging container and lists sheets"
    - path: "src/components/TaskCard.js"
      provides: "Interactive checklist items and fractional progress bar indicator"
    - path: "src/components/TaskEditModal.js"
      provides: "Category selection chip-row editor"
---

<objective>
Deliver the paging swiper sheets layout, subtask interactive checklist progress bar indicators, and metadata category selectors for KwestUp Mobile.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Build Swiper ViewPager Container</name>
  <files>src/screens/TaskListScreen.js</files>
  <action>
    - Refactor Task screen layout into a horizontal paging ScrollView.
    - Synchronize momentum scrolling indices with top category active states.
  </action>
  <acceptance_criteria>
    - Swiping pages snaps cleanly and transitions the top active selection.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Swipe paging swiper snaps beautifully.</done>
</task>

<task type="auto">
  <name>Task 2: Upgraded Task Card Progress Indicators</name>
  <files>src/components/TaskCard.js</files>
  <action>
    - Refactor task card subtask progress display to output fraction `completedCount / totalCount`.
    - Setup scale-spring checkboxes (`react-native-reanimated`) to support inline toggles.
  </action>
  <acceptance_criteria>
    - TaskCard displays progress text (e.g. 1/3 completed) next to progress bar.
    - Inline toggles work and immediately update parent fraction.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Fractional progress tracking and interactive checklists are fully complete.</done>
</task>

<task type="auto">
  <name>Task 3: Category Selector Chips in Edit Modal</name>
  <files>src/components/TaskEditModal.js</files>
  <action>
    - Setup horizontal category scrollable row chips under title/description in meta editor.
    - Pass active selection on save to relocate tasks between lists.
  </action>
  <acceptance_criteria>
    - TaskEditModal contains category select pills scrollbar.
    - Selecting a chip shifts the task to that list.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Tasks can be dynamically reallocated inside the meta editor.</done>
</task>

</tasks>
