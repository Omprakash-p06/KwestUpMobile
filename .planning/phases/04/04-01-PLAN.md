---
phase: 04-qr-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, app.json, src/screens/SettingsScreen.js]
autonomous: true
requirements: [SYNC-01]
must_haves:
  truths:
    - "expo-camera dependency is installed and registered in app.json"
    - "QR Scanner UI component supports permissions and manual fallback entries"
    - "SettingsScreen features a visual sync trigger button displaying last sync status"
  artifacts:
    - path: "src/components/QRScannerModal.js"
      provides: "QR barcode scanner camera view and credentials setup form"
---

<objective>
Integrate the native camera QR code scanning module, build the connection configuration modal, and establish the user settings trigger UI.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04/04-CONTEXT.md
@.planning/phases/04/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install expo-camera and Configure app.json</name>
  <files>package.json, app.json</files>
  <read_first>
    - package.json
    - app.json
  </read_first>
  <action>
    - Run the native installation command: `npx expo install expo-camera` using run_command.
    - Add the "expo-camera" configuration plugin inside `app.json` config plugins block:
      `["expo-camera", { "cameraPermission": "Allow KwestUp to access your camera to scan synchronization QR codes.", "barcodeScannerEnabled": true }]`
  </action>
  <acceptance_criteria>
    - package.json contains "expo-camera" in dependencies.
    - app.json includes the "expo-camera" plugin configuration with cameraPermission string.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Camera dependencies and native permission configurations are completed.</done>
</task>

<task type="auto">
  <name>Task 2: Build QR Scanner Modal Component</name>
  <files>src/components/QRScannerModal.js</files>
  <read_first>
    - src/utils/storage.js
  </read_first>
  <action>
    - Create a new file `src/components/QRScannerModal.js` using write_to_file.
    - Implement `QRScannerModal` as a standard React Native overlay using `react-native-modal`.
    - It must import `CameraView` and `useCameraPermissions` from `expo-camera`.
    - Provide an elegant overlay layout with a highlighted scanner target container, camera permission checking flow, loading loaders, and "Cancel" buttons.
    - Include a "Manual Entry" toggle button that switches the camera layout into a structured manual text input form (fields: PC IP Address, Port Number, Security Token) so developers can easily test sync on simulators.
    - Provide barcode scanning callbacks that parse scanned QR string as JSON, validate required fields (`ip`, `port`, `token`), haptically prompt success/failure, and call `onConnectionScanned(config)` on success.
  </action>
  <acceptance_criteria>
    - `src/components/QRScannerModal.js` exists.
    - Component uses `CameraView` and hooks permissions gracefully.
    - Component includes a togglable Manual Connection entry form.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>QR Scanner component with manual fallback setup is built and verified.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Scanner trigger in Settings Screen</name>
  <files>src/screens/SettingsScreen.js</files>
  <read_first>
    - src/screens/SettingsScreen.js
  </read_first>
  <action>
    - Update `src/screens/SettingsScreen.js` to render the `QRScannerModal` overlay.
    - Add a styled button "Synchronize with PC" inside the Settings list cards (using currentTheme styled layout, prefixing a Sync icon).
    - Render the last synchronization timestamp next to or underneath the button: e.g. "Last Synced: Yesterday, 14:02" or "Not Connected".
    - Hook the button press to launch the `QRScannerModal`.
    - Add a dummy hook callback `handleConnectionScanned(config)` that logs the retrieved credentials configuration on scan completion.
  </action>
  <acceptance_criteria>
    - SettingsScreen.js imports `QRScannerModal`.
    - SettingsScreen.js contains a "Synchronize with PC" button.
    - Pressing the sync button opens the `QRScannerModal` overlay.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>QR Scanning view is successfully integrated inside SettingsScreen.</done>
</task>

</tasks>

<success_criteria>
- Native `expo-camera` fully registered.
- `QRScannerModal` component provides camera feeds and manual text fallbacks.
- Settings screen features a modern trigger button displaying sync diagnostics.
</success_criteria>

<output>
After completion, create `.planning/phases/04/04-01-SUMMARY.md`
</output>
