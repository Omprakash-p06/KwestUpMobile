---
phase: 04-qr-sync
plan: 02
type: execute
wave: 1
depends_on: [04-01-PLAN.md]
files_modified: [App.js, src/screens/SettingsScreen.js]
autonomous: true
requirements: [SYNC-02]
must_haves:
  truths:
    - "Sync service sends local notes, tasks, task lists, and birthdays to PC sync server over HTTP"
    - "Conflict merge logic successfully writes returning payload into local AsyncStorage and device filesystem notes folder"
    - "Notifications and local reminder configurations are updated automatically after synchronization completes"
  artifacts:
    - path: "src/utils/syncService.js"
      provides: "HTTP REST API client coordinating Wi-Fi sync handshakes"
---

<objective>
Develop the HTTP REST client sync client, implement bidirectional data overwriting workflows, and integrate notes filesystem synchronization.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04/04-CONTEXT.md
@.planning/phases/04/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Client Sync Manager utility</name>
  <files>src/utils/syncService.js</files>
  <read_first>
    - src/utils/fileStorage.js
    - src/utils/storage.js
  </read_first>
  <action>
    - Create a new file `src/utils/syncService.js` using write_to_file.
    - Implement `performSync(config, localData)`:
      1. Compose connection URL: `const baseUrl = "http://" + config.ip + ":" + config.port;`
      2. Issue a short-timeout `fetch` GET request to `${baseUrl}/ping` to verify local network connectivity.
      3. Package the local payload: `{ notes: localData.notes, tasks: localData.tasks, taskLists: localData.taskLists, birthdays: localData.birthdays, themeMode: localData.themeMode, selectedThemeName: localData.selectedThemeName, userName: localData.userName }`.
      4. Issue a POST request to `${baseUrl}/sync` with headers `{ "Content-Type": "application/json", "Authorization": "Bearer " + config.token }` passing the packaged payload string.
      5. Read, parse, and return the resolved merged response JSON on success.
  </action>
  <acceptance_criteria>
    - `src/utils/syncService.js` exists.
    - It uses fetch API to perform GET /ping and POST /sync requests.
    - Headers contain the parsed token as Bearer authorization.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>HTTP sync client utility is established.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Sync merging and Filesystem Overwrites in App.js</name>
  <files>App.js</files>
  <read_first>
    - App.js
    - src/utils/fileStorage.js
  </read_first>
  <action>
    - Import `performSync` from `./src/utils/syncService` inside App.js.
    - Add a state hook `const [lastSynced, setLastSynced] = useState(null);` to track last successful sync datetime, loading it from AsyncStorage on app initialization.
    - Implement an asynchronous handler `handleExecuteSync(config)` in App.js passed down to AppNavigator and SettingsScreen:
      1. Set sync loading state.
      2. Call `performSync` with QR scanner config and local states (`{ notes, tasks, taskLists, birthdays, themeMode, selectedThemeName, userName }`).
      3. On successful resolution of merged payload:
         - Wipe old notes folder in filesystem via `wipeNotesFilesystem()`.
         - Write each note in the returned notes list to disk using `saveNoteFile(note.folder, note.title, note.content)`.
         - Update React states: `setTasks(result.tasks || [])`, `setTaskLists(result.taskLists || [])`, `setBirthdays(result.birthdays || [])`, `setThemeMode(result.themeMode || "light")`, `setSelectedThemeName(result.selectedThemeName || "dribbble")`, `setUserName(result.userName || "")`.
         - Reload notes state from filesystem by executing `getAllNotesFromFilesystem()`.
         - Re-schedule/cancel birthday alerts based on the new synced list.
         - Save current timestamp to AsyncStorage `kwestup_last_sync` and update `lastSynced` state.
         - Return success signal.
  </action>
  <acceptance_criteria>
    - App.js imports `performSync`.
    - App.js contains the `handleExecuteSync` callback.
    - Sync callback wipes notes filesystem, saves returning markdown files, updates storage, and schedules updated birthday notifications.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Sync merging engine and filesystem overwriting is fully integrated in App.js.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor Settings UI and Connect trigger handlers</name>
  <files>src/screens/SettingsScreen.js</files>
  <read_first>
    - src/screens/SettingsScreen.js
  </read_first>
  <action>
    - Pass `handleExecuteSync` and `lastSynced` down from App.js through `AppNavigator` to `SettingsScreen.js`.
    - Render a clean visual loader overlay (ActivityIndicator) on SettingsScreen while synchronization is in progress.
    - In `SettingsScreen.js`, wire the `QRScannerModal` scanned handler to trigger `handleExecuteSync(config)`.
    - Display beautiful toast messages or alerts informing users of completion status ("Synchronization succeeded! X tasks, Y notes updated" or "Connection failed. Please check local Wi-Fi subnet").
    - Render the formatted date-time string of `lastSynced` dynamically in the settings cell.
  </action>
  <acceptance_criteria>
    - SettingsScreen triggers `handleExecuteSync` callback on QR scan connection success.
    - Dynamic loading indicators and toast completion dialogs render correctly.
    - Sync timestamp is formatted and displayed in settings.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Settings UI sync actions are complete and user feedback is fully integrated.</done>
</task>

</tasks>

<success_criteria>
- Client `syncService` handles Rest API handshakes.
- App state coordinates safe cascading merges and overrides files.
- UI outputs real-time loading frames and validation toasts.
</success_criteria>

<output>
After completion, create `.planning/phases/04/04-02-SUMMARY.md`
</output>
