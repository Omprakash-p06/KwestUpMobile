---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, jest.config.js, app/_layout.tsx, app/(tabs)/_layout.tsx, app/(tabs)/tasks/index.tsx, app/(tabs)/finance/index.tsx, app/(tabs)/goals/index.tsx, src/store/storage.ts, src/store/useAppStore.ts]
autonomous: true
requirements: [PERF-01]
must_haves:
  truths:
    - "App boots into a tab-based navigation system (Tasks, Finance, Goals)"
    - "Global state persists across app reloads via MMKV"
    - "Tests can be executed via 'npm test'"
  artifacts:
    - path: "jest.config.js"
      provides: "Test runner configuration"
    - path: "app/_layout.tsx"
      provides: "Root navigation and provider setup"
    - path: "src/store/storage.ts"
      provides: "Zustand-MMKV persistence adapter"
  key_links:
    - from: "src/store/useAppStore.ts"
      to: "src/store/storage.ts"
      via: "persist middleware"
---

<objective>
Setup the core architectural foundation including navigation, persistent state management, and a testing framework. This plan upgrades the project to Expo SDK 55 and implements the New Architecture (Bridgeless Mode).
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/PHASE-1/RESEARCH.md
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testing & SDK Foundation</name>
  <files>package.json, jest.config.js</files>
  <action>
    - Upgrade Expo to SDK 55: `npx expo install expo@55.0.24 --fix`.
    - Install Testing dependencies: `npm install --save-dev jest jest-expo @testing-library/react-native @testing-library/jest-native`.
    - Create `jest.config.js` with `preset: 'jest-expo'`.
    - Update `package.json` with `"test": "jest"`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Project is on SDK 55 and an empty test run passes (or fails with "no tests found" correctly).</done>
</task>

<task type="auto">
  <name>Task 2: Implement Expo Router Navigation</name>
  <files>app/_layout.tsx, app/(tabs)/_layout.tsx, app/(tabs)/tasks/index.tsx, app/(tabs)/finance/index.tsx, app/(tabs)/goals/index.tsx</files>
  <action>
    - Install Expo Router: `npx expo install expo-router react-native-safe-area-context react-native-screens expo-linking expo-constants expo-status-bar`.
    - Setup `app/` directory:
      - `app/_layout.tsx`: Root layout using `Stack`.
      - `app/(tabs)/_layout.tsx`: Tab layout using `Tabs` from `expo-router`.
      - Create index files for Tasks, Finance, and Goals tabs.
    - Remove old `App.js` if it conflicts with Expo Router entry.
  </action>
  <verify>
    <automated>npx expo-typecheck</automated>
  </verify>
  <done>App structure follows file-based routing and compiles without type errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persistent State Management</name>
  <files>src/store/storage.ts, src/store/useAppStore.ts, src/store/__tests__/useAppStore.test.ts</files>
  <behavior>
    - Test 1: Store should initialize with default values.
    - Test 2: Store should update state correctly.
    - Test 3: Store should use MMKV for persistence (mocked).
  </behavior>
  <action>
    - Install Zustand and MMKV: `npx expo install zustand react-native-mmkv`.
    - Create `src/store/storage.ts`: Implement `StateStorage` interface for `react-native-mmkv`.
    - Create `src/store/useAppStore.ts`: Use `persist` middleware with the MMKV adapter.
    - Implement a simple `isFirstLaunch` or `theme` state to test persistence.
  </action>
  <verify>
    <automated>npm test src/store/__tests__/useAppStore.test.ts</automated>
  </verify>
  <done>Zustand store is functional and tests pass.</done>
</task>

</tasks>

<success_criteria>
- Project upgraded to SDK 55.
- Expo Router tabs functioning.
- Zustand store persists data via MMKV.
- Jest testing suite is operational.
</success_criteria>

<output>
After completion, create `.planning/phases/01/01-01-SUMMARY.md`
</output>
