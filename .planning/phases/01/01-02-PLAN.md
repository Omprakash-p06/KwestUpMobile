---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/db/client.ts, src/db/schema.ts, src/db/repositories/TaskRepository.ts, src/components/common/HighPerfList.tsx, app/(tabs)/tasks/index.tsx]
autonomous: true
requirements: [PERF-02]
must_haves:
  truths:
    - "SQLite database is initialized and accessible across the app"
    - "Relational data (Tasks) can be created and queried using repositories"
    - "Long lists scroll smoothly via FlashList optimization"
  artifacts:
    - path: "src/db/client.ts"
      provides: "Synchronous SQLite JSI connection"
    - path: "src/db/repositories/TaskRepository.ts"
      provides: "CRUD operations for tasks"
    - path: "src/components/common/HighPerfList.tsx"
      provides: "Reusable performance-optimized list component"
  key_links:
    - from: "src/db/repositories/TaskRepository.ts"
      to: "src/db/client.ts"
      via: "db.runSync/db.getAllSync"
    - from: "app/(tabs)/tasks/index.tsx"
      to: "src/components/common/HighPerfList.tsx"
      via: "component usage"
---

<objective>
Integrate high-performance local storage using expo-sqlite and optimize list rendering with FlashList to ensure smooth operation on budget Android devices.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/PHASE-1/RESEARCH.md
@.planning/phases/01/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Initialize SQLite Infrastructure</name>
  <files>src/db/client.ts, src/db/schema.ts, src/db/repositories/TaskRepository.ts, src/db/__tests__/TaskRepository.test.ts</files>
  <behavior>
    - Test 1: TaskRepository.add should insert a task into the database.
    - Test 2: TaskRepository.getAll should return all inserted tasks.
    - Test 3: Operations should be performant (JSI-based).
  </behavior>
  <action>
    - Install expo-sqlite: `npx expo install expo-sqlite`.
    - Create `src/db/client.ts`: Initialize the database using `SQLite.openDatabaseSync('kwestup.db')`.
    - Create `src/db/schema.ts`: Define the initial SQL schema for `tasks` table.
    - Create `src/db/repositories/TaskRepository.ts`: Implement `getAll`, `add`, and `delete` using `db.runSync` and `db.getAllSync`.
    - Note: Follow the Repository Pattern as recommended in RESEARCH.md.
  </action>
  <verify>
    <automated>npm test src/db/__tests__/TaskRepository.test.ts</automated>
  </verify>
  <done>SQLite repository is functional and verified with unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Build Performance-First List Component</name>
  <files>src/components/common/HighPerfList.tsx</files>
  <action>
    - Install FlashList: `npx expo install @shopify/flash-list`.
    - Create `src/components/common/HighPerfList.tsx`: 
      - Wrap `@shopify/flash-list`.
      - Expose standard `FlashList` props.
      - Enforce `estimatedItemSize` as a required prop to prevent common performance pitfalls.
      - Add basic error boundary or empty state handling.
  </action>
  <verify>
    <automated>npx expo-typecheck</automated>
  </verify>
  <done>Reusable HighPerfList component is created and type-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Integration & Performance Smoke Test</name>
  <files>app/(tabs)/tasks/index.tsx</files>
  <action>
    - Update `app/(tabs)/tasks/index.tsx` to:
      - Fetch data from `TaskRepository` on mount.
      - Use `HighPerfList` to render tasks.
      - Add a button to bulk-add 50 test tasks to verify scroll performance.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Tasks tab successfully renders data from SQLite using HighPerfList.</done>
</task>

</tasks>

<success_criteria>
- SQLite database operational with repository pattern.
- HighPerfList integrated and used in Tasks tab.
- Bulk data operations verified for performance.
</success_criteria>

<output>
After completion, create `.planning/phases/01/01-02-SUMMARY.md`
</output>
