# Plan: 07-01-PLAN - Skia Integration & Base Components

## Goal
Install Shopify React Native Skia package and implement the reusable hardware-accelerated `LiquidGlassCard` component and `LiquidGlassBackground` dynamic gradients container.

## Tasks
- [ ] Install package `@shopify/react-native-skia` via Expo CLI.
- [ ] Create `src/components/LiquidGlassCard.js` implementing BackdropFilter clipping bounds and custom blend matrix.
- [ ] Create `src/components/LiquidGlassBackground.js` implementing animated fluid background blobs.
- [ ] Verify components compile cleanly.

## Verification
- Run eslint checks to ensure zero static syntax errors.
