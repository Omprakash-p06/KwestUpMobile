---
phase: 10-industrial-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - App.js
  - src/theme/colors.js
  - src/theme/styles.js
  - src/components/LiquidGlassBackground.js
  - src/components/LiquidGlassCard.js
autonomous: true
requirements:
  - UI-02
user_setup:
  - service: "Metro bundler & Simulator"
    why: "Verification of font loading and skeuomorphic component rendering"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "Application successfully boots up loading Hanken Grotesk and JetBrains Mono fonts"
    - "Monochromatic Palette defines surface-bright, surface-dim, primary, and secondary grays/whites"
    - "Card component renders perfect sharp 90-degree square corners (0px border-radius) with raised bevel highlights"
    - "Background component renders brushed metal reflections in Dark Mode, textured paper in Light Mode, and obsidian black in AMOLED Mode"
  artifacts:
    - path: "src/theme/colors.js"
      provides: "New color tokens for KwestUp Monochromatic Industrial UI"
      min_lines: 50
    - path: "src/theme/styles.js"
      provides: "Updated injectFontFamily helper mapping to Hanken Grotesk and JetBrains Mono weights"
      min_lines: 60
    - path: "src/components/LiquidGlassBackground.js"
      provides: "Brushed steel linear-gradient, paper backgrounds, and stardust grain overlay"
      min_lines: 40
    - path: "src/components/LiquidGlassCard.js"
      provides: "Sharp square edges, 1px highlight top-left bevels, 2px shadow bottom-right, and corner rivet screw slots"
      min_lines: 100
  key_links: []

---

<objective>
Install `@expo-google-fonts/hanken-grotesk` and `@expo-google-fonts/jetbrains-mono`, load custom fonts inside App.js, and refactor color systems, styling helper rules, card layouts, and background systems to map to the strict, premium, high-contrast industrial skeuomorphic design system.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10/10-RESEARCH.md
@/UI Design plan/kwestup/DESIGN.md
@App.js
@src/theme/colors.js
@src/theme/styles.js
@src/components/LiquidGlassBackground.js
@src/components/LiquidGlassCard.js
</context>

<interfaces>
Theme colors defined in DESIGN.md:
- surface: '#131313'
- surface-bright: '#393939'
- surface-container: '#20201f'
- primary: '#ffffff'
- on-primary: '#2f3131'
- outline: '#8e9192'
- background: '#131313'
- on-background: '#e5e2e1'
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install font packages & load custom fonts in App.js</name>
  <files>
    - package.json
    - App.js
  </files>
  <read_first>
    - App.js lines 10-20 (font packages imports)
    - App.js lines 58-64 (fontsLoaded state)
  </read_first>
  <action>
    1. Install `@expo-google-fonts/hanken-grotesk` and `@expo-google-fonts/jetbrains-mono` dependencies:
       ```bash
       npx expo install @expo-google-fonts/hanken-grotesk @expo-google-fonts/jetbrains-mono
       ```
    2. Import the new font weights inside `App.js`:
       - `HankenGrotesk_400Regular`, `HankenGrotesk_500Medium`, `HankenGrotesk_700Bold`, `HankenGrotesk_800ExtraBold` from `@expo-google-fonts/hanken-grotesk`
       - `JetBrainsMono_400Regular`, `JetBrainsMono_500Medium`, `JetBrainsMono_700Bold` from `@expo-google-fonts/jetbrains-mono`
    3. Modify `useFonts` hook inside `App.js` to load:
       - `HankenGrotesk-Regular`: `HankenGrotesk_400Regular`
       - `HankenGrotesk-Medium`: `HankenGrotesk_500Medium`
       - `HankenGrotesk-Bold`: `HankenGrotesk_700Bold`
       - `HankenGrotesk-ExtraBold`: `HankenGrotesk_800ExtraBold`
       - `JetBrainsMono-Regular`: `JetBrainsMono_400Regular`
       - `JetBrainsMono-Medium`: `JetBrainsMono_500Medium`
       - `JetBrainsMono-Bold`: `JetBrainsMono_700Bold`
  </action>
  <verify>
    <automated>
      grep -q "hanken-grotesk" package.json && echo "✓ Hanken Grotesk package installed"
      grep -q "jetbrains-mono" package.json && echo "✓ JetBrains Mono package installed"
      grep -q "HankenGrotesk-ExtraBold" App.js && echo "✓ Custom fonts configured inside App.js useFonts"
    </automated>
  </verify>
  <acceptance_criteria>
    - Font packages appear inside `package.json` dependencies.
    - `App.js` imports font weights and successfully loads them under custom names.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Refactor theme colors & styles.js font-injector rules</name>
  <files>
    - src/theme/colors.js
    - src/theme/styles.js
  </files>
  <read_first>
    - src/theme/colors.js (dribbbleColors, themes mapping)
    - src/theme/styles.js (injectFontFamily helper)
  </read_first>
  <action>
    1. Completely rewrite `src/theme/colors.js` to reflect the industrial monochromatic colors:
       - **Light Mode**: Background `#E4E2E1`, cardBackground `#FFFFFF`, text `#000000`, border `#8E9192`.
       - **Dark Mode**: Background `#131313`, cardBackground `#20201F`, text `#FFFFFF`, border `#8E9192`.
       - **AMOLED Mode**: Background `#000000`, cardBackground `#0E0E0E`, text `#E5E2E1`, border `#353535`.
    2. Rewrite `injectFontFamily` inside `src/theme/styles.js`:
       - If `fontWeight` matches `bold` / `700` / `800` / `900`, assign `fontFamily = "HankenGrotesk-ExtraBold"`.
       - If `fontWeight` is `600`, assign `fontFamily = "HankenGrotesk-Bold"`.
       - If `fontWeight` is `500`, assign `fontFamily = "HankenGrotesk-Medium"`.
       - Else assign `fontFamily = "HankenGrotesk-Regular"`.
       - If element styles explicitly target tech labels / timestamps / timers, force `fontFamily = "JetBrainsMono-Medium"`.
  </action>
  <verify>
    <automated>
      grep -q "HankenGrotesk-ExtraBold" src/theme/styles.js && echo "✓ Styles font-injector successfully refactored"
      grep -q "131313" src/theme/colors.js && echo "✓ Colors system successfully updated with industrial palette"
    </automated>
  </verify>
  <acceptance_criteria>
    - Theme palettes completely match monochromatic spec.
    - Global font injector maps text fields to Hanken Grotesk families.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Refactor LiquidGlassBackground and LiquidGlassCard skeuomorphic styles</name>
  <files>
    - src/components/LiquidGlassBackground.js
    - src/components/LiquidGlassCard.js
  </files>
  <read_first>
    - src/components/LiquidGlassBackground.js
    - src/components/LiquidGlassCard.js
  </read_first>
  <action>
    1. Redesign `LiquidGlassBackground.js`:
       - Import `LinearGradient` from `expo-linear-gradient`.
       - If theme is Dark: Render the brushed metal horizon LinearGradient: `['#131313', '#20201f', '#2a2a2a', '#20201f', '#131313']` horizontally.
       - If theme is Light: Render warm linen/parchment desk background (`https://images.unsplash.com/photo-1586075010923-2dd4570fb338?q=80&w=1200&auto=format&fit=crop`).
       - If theme is AMOLED: Render pure black.
       - Overlay the subtle 5% transparent stardust texture (`https://www.transparenttextures.com/patterns/stardust.png`) as a grain overlay across all screens!
    2. Redesign `LiquidGlassCard.js`:
       - Force perfect `borderRadius: 0` sharp angles across all cards.
       - Implement Raised Outward Bevel depth offset:
         - Top-left border highlight overlay (`borderTopWidth: 1`, `borderLeftWidth: 1`, `borderTopColor: 'rgba(255,255,255,0.2)'`, `borderLeftColor: 'rgba(255,255,255,0.2)'`).
         - Bottom-right shadow border offset (`borderBottomWidth: 2`, `borderRightWidth: 2`, `borderBottomColor: 'rgba(0,0,0,0.55)'`, `borderRightColor: 'rgba(0,0,0,0.55)'`).
       - If theme is Dark: render brushed aluminum plate texture overlay with corner rivets (4 gray screws at `{top: 10, left: 10}` etc.).
       - If theme is Light: render ruled ledger paper lines overlay, red vertical margin line, and silver ring binders.
  </action>
  <verify>
    <automated>
      grep -q "LinearGradient" src/components/LiquidGlassBackground.js && echo "✓ Background renders linear steel gradients"
      grep -q "borderRadius: 0" src/components/LiquidGlassCard.js && echo "✓ Card uses perfect sharp square corners"
    </automated>
  </verify>
  <acceptance_criteria>
    - Background successfully employs horizontal metal gradient in dark mode, parchment in light mode, and black in AMOLED.
    - Stardust noise texture overlays all backgrounds.
    - Card uses sharp 90-degree corners and displays top-left bevel offsets.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
No trust boundary shifts or dynamic user input expansions. All threats mitigated.
</threat_model>

<verification>
1. Verify `@expo-google-fonts/hanken-grotesk` & `@expo-google-fonts/jetbrains-mono` appear in package.json.
2. Verify `fontsLoaded` in `App.js` completes successfully.
3. Validate style compiler builds: `npx eslint src/components/LiquidGlassBackground.js src/components/LiquidGlassCard.js` returns 0 errors.
</verification>

<success_criteria>
- Custom fonts fully load.
- Core industrial themes, sharp cards, and gradient backgrounds render cleanly without Metro compilation errors.
</success_criteria>

<output>
After completion, create `.planning/phases/10/10-01-SUMMARY.md`
</output>
