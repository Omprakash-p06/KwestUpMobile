---
phase: 09-android-widgets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - widgets/FocusTimerWidget.tsx
  - widgets/DailyTasksWidget.tsx
  - widgets/widget-task-handler.tsx
  - app.json
autonomous: true
requirements:
  - WIDG-01
user_setup:
  - service: "Android device/emulator"
    why: "Widgets render on Android home screen — cannot test on iOS web or Expo Go"
    env_vars: []
    dashboard_config:
      - task: "Enable Developer Options + USB Debugging on Android device"
        location: "Settings → About Phone → Tap Build Number 7x → Settings → Developer Options → USB Debugging"

must_haves:
  truths:
    - "Android widget picker lists 'Focus Timer' and 'Daily Tasks' widgets after dev client build"
    - "Focus Timer widget shows MM:SS countdown when timer is running in app"
    - "Daily Tasks widget shows completed/total task count"
    - "Widget task handler reads from AsyncStorage so widgets show data on cold start"
  artifacts:
    - path: "widgets/FocusTimerWidget.tsx"
      provides: "Focus Timer widget component using Widget primitives (FlexWidget, TextWidget)"
      min_lines: 30
    - path: "widgets/DailyTasksWidget.tsx"
      provides: "Daily Tasks widget component with progress display"
      min_lines: 30
    - path: "widgets/widget-task-handler.tsx"
      provides: "Widget lifecycle handler — maps widget names to components, handles WIDGET_ADDED, WIDGET_UPDATE, WIDGET_DELETED"
      min_lines: 40
    - path: "app.json"
      provides: "Widget registration in Expo config plugin — defines FocusTimer and DailyTasks widgets"
      contains: "react-native-android-widget"
  key_links:
    - from: "widgets/widget-task-handler.tsx"
      to: "widgets/FocusTimerWidget.tsx"
      via: "import"
      pattern: "import.*FocusTimerWidget"
    - from: "widgets/widget-task-handler.tsx"
      to: "widgets/DailyTasksWidget.tsx"
      via: "import"
      pattern: "import.*DailyTasksWidget"
    - from: "widgets/widget-task-handler.tsx"
      to: "src/utils/storage"
      via: "import STORAGE_VERSION"
      pattern: "import.*STORAGE_VERSION"

---

<objective>
Install `react-native-android-widget`, create the two widget components (FocusTimer, DailyTasks), build the widget task handler for lifecycle management, and register widgets in the Expo config plugin.

Purpose: Establish the widget infrastructure so Android home-screen widgets can render data from the app's existing AsyncStorage persistence.
Output: Widget component files, task handler, and Expo config plugin registration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09/09-RESEARCH.md (Standard Stack, Architecture Patterns, Code Examples, Common Pitfalls)
@.planning/REQUIREMENTS.md (WIDG-01)
@.planning/PROJECT.md
@.planning/phases/08/08-SUMMARY.md
@App.js (existing app state — dailyTasks, timerRemaining, isTimerRunning)
@src/utils/storage.js (STORAGE_VERSION export)
@app.json (existing Expo config — to modify plugins array)
</context>

<interfaces>
<!-- Existing exports from storage.js that widget-task-handler needs -->

From src/utils/storage.js:
```javascript
export const APP_VERSION = "v2.5.0-SIDE-PANEL-REDESIGN";
export const STORAGE_VERSION = "v5.0";
```

AsyncStorage data shape (saved under key `kwestup_data_${STORAGE_VERSION}`):
```json
{
  "dailyTasks": [{"completed": true/false, ...}],
  "timerState": {
    "duration": 1500,
    "remaining": 1200,
    "isRunning": true/false
  },
  "tasks": [...],
  ...
}
```

Existing app.json plugins array (to append widget config plugin):
```json
"plugins": [
  ["expo-camera", { "cameraPermission": "..." }],
  ["expo-build-properties", { "ios": { "useFrameworks": "static" } }]
]
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install react-native-android-widget + Create widget component files</name>

  <files>
    - package.json (modified — dependency added)
    - widgets/FocusTimerWidget.tsx (created)
    - widgets/DailyTasksWidget.tsx (created)
  </files>

  <read_first>
    - App.js lines 56-58 (timerRemaining, isTimerRunning state)
    - app.json (check existing plugins and android config)
    - RESEARCH.md lines 166-211 (Pattern 1: Widget Component using Primitives, code examples)
  </read_first>

  <action>
    1. Install the library:
       ```bash
       npx expo install react-native-android-widget
       ```
       Verify it appears in package.json dependencies after install.

    2. Create directory `widgets/` at project root if it doesn't exist.

    3. Create `widgets/FocusTimerWidget.tsx`:
       - Add `'use no memo';` at top to prevent React Compiler hook injection
       - Import `React` from 'react'
       - Import `FlexWidget, TextWidget` from 'react-native-android-widget'
       - Export function `FocusTimerWidget` accepting props: `remaining: number, isRunning: boolean`
       - Calculate minutes and seconds from `remaining` (seconds only input):
         ```typescript
         const minutes = Math.floor(remaining / 60);
         const secs = remaining % 60;
         ```
       - Format as `MM:SS` with zero padding
       - Return a FlexWidget with:
         - `height: 'match_parent', width: 'match_parent'`
         - `justifyContent: 'center', alignItems: 'center'`
         - `backgroundColor: '#1a1a2e'` (dark background for visibility on any wallpaper)
         - `borderRadius: 16, padding: 16`
         - `accessibilityLabel` showing the time string
       - Inside: a TextWidget for the time string (fontSize: 32, fontFamily: 'sans-serif-medium', color: '#ffffff')
       - Inside: a TextWidget for the running/paused status:
         - `isRunning ? '● Focus Active' : '○ Timer Paused'`
         - fontSize: 14, color: `isRunning ? '#4ade80' : '#94a3b8'`, marginTop: 4
       - The component must be a pure function — NO hooks, NO React Native components (View, Text)

    4. Create `widgets/DailyTasksWidget.tsx`:
       - Add `'use no memo';` at top
       - Import React, FlexWidget, TextWidget same as above
       - Export function `DailyTasksWidget` accepting props: `dailyTaskCount: number, dailyTasksCompleted: number`
       - Calculate percentage: `dailyTaskCount > 0 ? Math.round((dailyTasksCompleted / dailyTaskCount) * 100) : 0`
       - Return a FlexWidget with same sizing and `backgroundColor: '#0f172a'`, borderRadius: 16, padding: 16
       - Accessibility label: `Daily tasks: ${dailyTasksCompleted} of ${dailyTaskCount} completed`
       - Inside:
         - TextWidget "Daily Tasks" (fontSize: 14, color: '#94a3b8', marginBottom: 8)
         - TextWidget `${dailyTasksCompleted} / ${dailyTaskCount}` (fontSize: 36, fontWeight: 'bold')
           - Color: '#4ade80' if all tasks completed AND there are tasks, else '#ffffff'
         - If `dailyTaskCount > 0`: TextWidget `${progress}% complete` (fontSize: 12, color: progress===100 ? '#4ade80' : '#94a3b8', marginTop: 4)
  </action>

  <verify>
    <automated>
      # Verify package installed
      node -e "const p = require('./package.json'); console.assert(p.dependencies['react-native-android-widget'], 'Package not found'); console.log('✓ Package installed')"
      # Verify widget files exist
      test -f widgets/FocusTimerWidget.tsx && echo "✓ FocusTimerWidget.tsx exists"
      test -f widgets/DailyTasksWidget.tsx && echo "✓ DailyTasksWidget.tsx exists"
      # Verify no React Native components inside widgets
      ! grep -q "from 'react-native'" widgets/FocusTimerWidget.tsx && echo "✓ No RN imports in widget"
      ! grep -q "from 'react-native'" widgets/DailyTasksWidget.tsx && echo "✓ No RN imports in widget"
      # Verify 'use no memo' directive
      grep -q "'use no memo'" widgets/FocusTimerWidget.tsx && echo "✓ FocusTimer has no memo directive"
      grep -q "'use no memo'" widgets/DailyTasksWidget.tsx && echo "✓ DailyTasks has no memo directive"
      # Verify exports
      grep -q "export function FocusTimerWidget" widgets/FocusTimerWidget.tsx && echo "✓ FocusTimerWidget exported"
      grep -q "export function DailyTasksWidget" widgets/DailyTasksWidget.tsx && echo "✓ DailyTasksWidget exported"
    </automated>
  </verify>

  <acceptance_criteria>
    - `package.json` contains `"react-native-android-widget"` in dependencies
    - `widgets/FocusTimerWidget.tsx` exists with `export function FocusTimerWidget` and uses `FlexWidget`/`TextWidget` (not `View`/`Text`)
    - `widgets/DailyTasksWidget.tsx` exists with `export function DailyTasksWidget` and uses `FlexWidget`/`TextWidget`
    - Both files contain `'use no memo';` as first directive
    - Neither file imports from `react-native`
  </acceptance_criteria>

  <done>Package installed, widget component files created and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Create widget-task-handler with AsyncStorage reads</name>

  <files>
    - widgets/widget-task-handler.tsx (created)
  </files>

  <read_first>
    - RESEARCH.md lines 216-265 (Pattern 2: Widget Task Handler Registration, code example)
    - src/utils/storage.js (for STORAGE_VERSION export)
    - Task 1 output (FocusTimerWidget.tsx, DailyTasksWidget.tsx — verify component names and props)
  </read_first>

  <action>
    Create `widgets/widget-task-handler.tsx`:

    - Import React from 'react'
    - Import `type WidgetTaskHandlerProps` from 'react-native-android-widget'
    - Import AsyncStorage from '@react-native-async-storage/async-storage'
    - Import `FocusTimerWidget` from './FocusTimerWidget'
    - Import `DailyTasksWidget` from './DailyTasksWidget'
    - Import `STORAGE_VERSION` from '../src/utils/storage'

    - Create a `nameToWidget` map:
      ```typescript
      const nameToWidget = {
        FocusTimer: FocusTimerWidget,
        DailyTasks: DailyTasksWidget,
      } as const;
      ```

    - Export an async function `widgetTaskHandler(props: WidgetTaskHandlerProps)`:

      - Extract `widgetInfo` from props, get `widgetName`
      - Switch on `props.widgetAction`:

        - **case 'WIDGET_ADDED' | 'WIDGET_UPDATE':**
          1. Read from AsyncStorage: `const raw = await AsyncStorage.getItem(\`kwestup_data_\${STORAGE_VERSION}\`);`
          2. If raw data exists, parse it:
             ```typescript
             const parsed = JSON.parse(raw);
             widgetData = {
               timerRemaining: parsed.timerState?.remaining ?? 0,
               isTimerRunning: parsed.timerState?.isRunning ?? false,
               dailyTaskCount: (parsed.dailyTasks || []).length,
               dailyTasksCompleted: (parsed.dailyTasks || []).filter(t => t.completed).length,
             };
             ```
          3. Look up the Widget component from `nameToWidget[widgetName]`
          4. If found, call `props.renderWidget(<Widget {...widgetData} />)`

        - **case 'WIDGET_DELETED':** break (no-op for read-only widgets)
        - **case 'WIDGET_CLICK':** break (no deep linking per requirements)
        - **default:** break

      - **IMPORTANT:** Do NOT use lodash or any 3rd-party utility. Plain JS only.
      - **IMPORTANT:** Use `STORAGE_VERSION` dynamic key — do NOT hard-code the storage key to prevent version mismatch (Pitfall 5 from RESEARCH.md)
  </action>

  <verify>
    <automated>
      test -f widgets/widget-task-handler.tsx && echo "✓ widget-task-handler.tsx exists"
      grep -q "export async function widgetTaskHandler" widgets/widget-task-handler.tsx && echo "✓ widgetTaskHandler exported"
      grep -q "STORAGE_VERSION" widgets/widget-task-handler.tsx && echo "✓ Uses dynamic STORAGE_VERSION key"
      grep -q "FocusTimerWidget" widgets/widget-task-handler.tsx && echo "✓ Imports FocusTimerWidget"
      grep -q "DailyTasksWidget" widgets/widget-task-handler.tsx && echo "✓ Imports DailyTasksWidget"
      grep -q "requestWidgetUpdate" widgets/widget-task-handler.tsx && echo "✓ Has widget update path"
    </automated>
  </verify>

  <acceptance_criteria>
    - `widgets/widget-task-handler.tsx` exists with `export async function widgetTaskHandler`
    - Handler reads AsyncStorage using `kwestup_data_v5.0` key from STORAGE_VERSION
    - Handler maps widgetName to component for both FocusTimer and DailyTasks
    - Handler uses `props.renderWidget()` to update the widget
    - No hard-coded storage key (uses STORAGE_VERSION import)
    - No deep linking or click handling (read-only widgets per requirements)
  </acceptance_criteria>

  <done>Widget task handler created with AsyncStorage reads and lifecycle handling.</done>
</task>

<task type="auto">
  <name>Task 3: Update app.json with react-native-android-widget config plugin</name>

  <files>
    - app.json (modified — added widget plugin config)
  </files>

  <read_first>
    - RESEARCH.md lines 293-334 (Pattern 4: Expo Config Plugin Registration, code example)
    - RESEARCH.md lines 334-340 (Anti-Patterns to Avoid)
    - app.json (current state — must preserve all existing config)
  </read_first>

  <action>
    Modify the `plugins` array in `app.json` to add the `react-native-android-widget` config plugin.

    The existing plugins array is:
    ```json
    "plugins": [
      [
        "expo-camera",
        {
          "cameraPermission": "Allow KwestUp to access your camera to scan synchronization QR codes."
        }
      ],
      [
        "expo-build-properties",
        {
          "ios": {
            "useFrameworks": "static"
          }
        }
      ]
    ]
    ```

    Append a new plugin entry AFTER the expo-build-properties entry:
    ```json
    [
      "react-native-android-widget",
      {
        "fonts": [],
        "widgets": [
          {
            "name": "FocusTimer",
            "label": "Focus Timer",
            "description": "Shows your current focus session timer status",
            "minWidth": "250dp",
            "minHeight": "100dp",
            "targetCellWidth": 3,
            "targetCellHeight": 2,
            "previewImage": "./assets/widget-preview/focus-timer-preview.png",
            "updatePeriodMillis": 1800000
          },
          {
            "name": "DailyTasks",
            "label": "Daily Tasks",
            "description": "Shows your daily task completion progress",
            "minWidth": "250dp",
            "minHeight": "100dp",
            "targetCellWidth": 3,
            "targetCellHeight": 2,
            "previewImage": "./assets/widget-preview/daily-tasks-preview.png",
            "updatePeriodMillis": 1800000
          }
        ]
      }
    ]
    ```

    Keep all existing plugins and their configurations intact. Do NOT remove or modify anything else in app.json.

    Also create placeholder widget preview images directory:
    ```bash
    mkdir -p assets/widget-preview
    ```
    The preview images will be placeholder PNGs — they are not strictly required for widget functionality but the config plugin expects them.
  </action>

  <verify>
    <automated>
      grep -q "react-native-android-widget" app.json && echo "✓ Widget plugin registered in app.json"
      grep -q "FocusTimer" app.json && echo "✓ FocusTimer widget defined"
      grep -q "DailyTasks" app.json && echo "✓ DailyTasks widget defined"
      grep -q "expo-camera" app.json && echo "✓ Camera plugin preserved"
      grep -q "expo-build-properties" app.json && echo "✓ Build properties plugin preserved"
      test -d assets/widget-preview && echo "✓ Widget preview directory created"
      node -e "
        const cfg = require('./app.json');
        const plugins = cfg.expo.plugins;
        const widgetPlugin = plugins.find(p => Array.isArray(p) && p[0] === 'react-native-android-widget');
        console.assert(widgetPlugin, 'Widget plugin not found in plugins array');
        console.assert(widgetPlugin[1].widgets.length === 2, 'Expected 2 widgets');
        console.log('✓ JSON config validated');
      "
    </automated>
  </verify>

  <acceptance_criteria>
    - `app.json` plugins array contains the `react-native-android-widget` config plugin entry
    - Both FocusTimer and DailyTasks widgets are defined with the exact configuration shown above
    - All existing plugins (expo-camera, expo-build-properties) are preserved unchanged
    - Widget config includes correct minWidth/minHeight/targetCellWidth/targetCellHeight values
    - `assets/widget-preview/` directory exists
  </acceptance_criteria>

  <done>app.json updated with widget config plugin, both widgets registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → Widget | Widget task handler reads persisted app state from AsyncStorage for display |
| App.js (JS) → Widget | App.js calls `requestWidgetUpdate` to push foreground state changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure (S) | Widget display | accept | Widgets read the same AsyncStorage data already accessible by the app. No PII beyond what app already displays (task names, timer state). No network access from widget. No user input. Low-value target — risk accepted per RESEARCH.md Security Domain. |
| T-09-02 | Tampering | App.json plugin config | mitigate | Config plugin only registers widgets during prebuild — runtime changes to app.json require fresh dev client build. No runtime modification vector exists. |
</threat_model>

<verification>
1. `package.json` has `react-native-android-widget` dependency
2. `widgets/FocusTimerWidget.tsx` exists with `FlexWidget`/`TextWidget` primitives and `'use no memo'`
3. `widgets/DailyTasksWidget.tsx` exists with same constraints
4. `widgets/widget-task-handler.tsx` exists with `widgetTaskHandler` export and AsyncStorage reads
5. `app.json` has `react-native-android-widget` plugin entry with both widget definitions
6. Metro bundler compiles without errors: `npx expo export --platform android --dump-sourcemap`
</verification>

<success_criteria>
- Widget infrastructure in place: components, task handler, config plugin registration
- Metro bundler can compile the widget code (confirmed by successful `npx expo export --platform android`)
- Widgets ready for integration in Plan 09-02 (index.js + App.js wiring)
</success_criteria>

<output>
After completion, create `.planning/phases/09-android-widgets/09-01-SUMMARY.md`
</output>
