---
phase: 01-notes
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/navigation/CustomDrawerContent.js, src/navigation/AppNavigator.js, src/screens/NotesScreen.js]
autonomous: true
requirements: [NOTE-02, NOTE-03]
must_haves:
  truths:
    - "Sidebar folder explorer displays folder categorization and custom tags"
    - "Search bar actively filters note titles and snippets"
    - "Markdown editor supports key accessory toolbar inputs (headers, lists, bold)"
    - "Markdown renderer converts headers, bullets, and todo checklists beautifully"
  artifacts:
    - path: "src/screens/NotesScreen.js"
      provides: "Obsidian/Notion Markdown notes screen component"
---

<objective>
Design and implement a highly professional, Notion/Obsidian-style Markdown notes panel featuring a folding sidebar explorer, search, and a dual edit/preview rendering engine.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Notes Option to Custom Drawer Navigation</name>
  <files>src/navigation/CustomDrawerContent.js, src/navigation/AppNavigator.js</files>
  <read_first>
    - src/navigation/CustomDrawerContent.js
    - src/navigation/AppNavigator.js
  </read_first>
  <action>
    - Add the notebook drawer item: `{ key: "Notes", title: "Notes", icon: "notebook", route: "Notes" }` inside the drawerItems array in `CustomDrawerContent.js`.
    - In `AppNavigator.js`, import `NotesScreen` from `../screens/NotesScreen` and add a `Drawer.Screen` route for "Notes" inside the Drawer.Navigator component passing `notes` and `setNotes` props.
  </action>
  <acceptance_criteria>
    - src/navigation/CustomDrawerContent.js contains the key: `"Notes"` in drawerItems.
    - src/navigation/AppNavigator.js contains the import statement for `NotesScreen` and the `<Drawer.Screen name="Notes"` XML block.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Notes route is configured in Drawer Navigator.</done>
</task>

<task type="auto">
  <name>Task 2: Build Folders Sidebar & Search UI</name>
  <files>src/screens/NotesScreen.js</files>
  <read_first>
    - src/screens/NotesScreen.js
  </read_first>
  <action>
    - Extract folders and tags dynamically from notes array inside `NotesScreen.js` using `useMemo`.
    - Create a left sidebar explorer showing the folders list (with folder-adding button modal) and hashtags list.
    - Implement a note list displaying filtered cards with title, content snippet, tags, and date, accompanied by a search bar input.
  </action>
  <acceptance_criteria>
    - src/screens/NotesScreen.js contains folder/tag extraction via useMemo hooks.
    - src/screens/NotesScreen.js contains FlatList notes display and folder-adding modal with isFolderModalVisible state.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Explorer list and search functionality works perfectly.</done>
</task>

<task type="auto">
  <name>Task 3: Build Markdown Editor & Preview Engine</name>
  <files>src/screens/NotesScreen.js</files>
  <read_first>
    - src/screens/NotesScreen.js
  </read_first>
  <action>
    - Implement dual Edit/Preview mode tabs in `NotesScreen.js` toggling the `editorTab` state.
    - Edit Mode: Title input, folder/tags meta inputs, large body input, and a Markdown Toolbar supporting Headers (`#`), bold (`**`), italic (`*`), checklist (`- [ ]`), and bullets (`- `).
    - Preview Mode: Custom regex parsing line-by-line translating Markdown formatting to React Native style components.
  </action>
  <acceptance_criteria>
    - src/screens/NotesScreen.js contains markdown toolbar buttons with insertMarkdown triggers.
    - src/screens/NotesScreen.js contains renderMarkdown and parseInlineMarkdown text parsing functions.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Dual markdown editor and renderer is fully functional.</done>
</task>

</tasks>

<success_criteria>
- Notes navigation integrated in drawer.
- Collapsible Obsidian-style folder and tag navigation working.
- Note edit/preview rendering operational and error-free.
</success_criteria>

<output>
After completion, create `.planning/phases/01/01-02-SUMMARY.md`
</output>
