---
phase: 04-qr-sync
plan: 03
type: execute
wave: 1
depends_on: [04-02-PLAN.md]
files_modified: []
autonomous: true
requirements: [SYNC-02]
must_haves:
  truths:
    - "Python Flask server handles REST API calls securely"
    - "LWW conflict merge algorithm merges client-server arrays correctly on server side"
    - "Dynamic console print and HTML QR code rendering page is served under GET / route"
  artifacts:
    - path: "KwestUpPC/sync_server.py"
      provides: "Lightweight companion local synchronization server"
---

<objective>
Develop the reference Python companion sync server (`sync_server.py`) for desktop, configure dynamic console and HTML-based QR code render routes, and write verification testing helpers.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04/04-CONTEXT.md
@.planning/phases/04/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Develop the Reference Sync Server Script</name>
  <files>KwestUpPC/sync_server.py</files>
  <read_first>
    - KwestUpPC/README.md
  </read_first>
  <action>
    - Create `KwestUpPC/sync_server.py` using write_to_file.
    - Implement a lightweight Python script using **Flask**.
    - Implement dynamic IP extraction mapping using standard UDP socket binding.
    - Declare standard storage persistence file `kwestup_pc_vault.json` loaded and saved cleanly via standard JSON utilities.
    - Implement two-way LWW merge engine logic consolidating: Notes (matched by unique file path `id`), Tasks (matched by key `id`), Task Lists, and Birthdays.
    - Set up endpoints:
      - `GET /ping`: simple JSON status check.
      - `POST /sync`: authenticates with Ephemeral Token matching, executes LWW merging, updates PC file storage, and returns JSON.
  </action>
  <acceptance_criteria>
    - `KwestUpPC/sync_server.py` exists.
    - Server executes Flask listening on all interfaces (`host='0.0.0.0'`, `port=5001`).
    - `/sync` endpoint validates Bearer tokens.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m py_compile KwestUpPC/sync_server.py</automated>
  </verify>
  <done>Companion Flask server script is established.</done>
</task>

<task type="auto">
  <name>Task 2: Build dynamic HTML QR Code Render Route</name>
  <files>KwestUpPC/sync_server.py</files>
  <read_first>
    - KwestUpPC/sync_server.py
  </read_first>
  <action>
    - Update `KwestUpPC/sync_server.py` to add a route `GET /` returning a beautiful, responsive HTML page.
    - The HTML page must render the QR code dynamically using a client-side JavaScript QR library (like `davidshimjs/qrcodejs` or standard CDN `cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js`).
    - Render host configurations: Local IP, Server Port, and Access Token visually with smooth gradients, dark mode, and step-by-step instructions guiding users to scan with KwestUp Mobile.
    - Render a plain-text payload string so developers can copy/paste if testing on a simulator.
  </action>
  <acceptance_criteria>
    - `KwestUpPC/sync_server.py` contains a `/` route rendering HTML.
    - The served HTML displays the QR code dynamic script loader.
    - Host parameters are presented clearly in layout.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m py_compile KwestUpPC/sync_server.py</automated>
  </verify>
  <done>Dynamic QR code web interface is built and integrated.</done>
</task>

<task type="auto">
  <name>Task 3: Create Integration test helper</name>
  <files>KwestUpPC/test_sync.py</files>
  <read_first>
    - KwestUpPC/sync_server.py
  </read_first>
  <action>
    - Create a test script `KwestUpPC/test_sync.py` using write_to_file.
    - Write a clean validation script simulating REST queries: pings the Flask server, performs a sync simulation using dummy datasets, validates that conflict resolution accurately isolates the most recently updated items, and reports success.
  </action>
  <acceptance_criteria>
    - `KwestUpPC/test_sync.py` exists.
    - Test script compiles and is ready for automated execution.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m py_compile KwestUpPC/test_sync.py</automated>
  </verify>
  <done>Sync test runner is complete and verified.</done>
</task>

</tasks>

<success_criteria>
- Companion `sync_server.py` runs Flask successfully.
- Webpage serving GET `/` generates high-quality QR codes.
- Local tests pass compilation verifying LWW conflict merges.
</success_criteria>

<output>
After completion, create `.planning/phases/04/04-03-SUMMARY.md`
</output>
