---
phase: 10-industrial-ui
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/CustomButton.js
  - src/components/AIAssistant.js
  - src/screens/NotesScreen.js
  - src/components/TaskCard.js
autonomous: true
requirements:
  - UI-03
user_setup:
  - service: "Metro bundler & Simulator"
    why: "Verification of animations and button toggles"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "Buttons render as perfect rectangles (0px border-radius) and invert their colors on press"
    - "Checked note checklists render an X mark inside a square rather than a standard checkmark"
    - "AI component renders a sliding horizontal laser scan animation that runs continuously"
  artifacts:
    - path: "src/components/CustomButton.js"
      provides: "Mechanical custom block button with color-inversion active effect"
      min_lines: 40
    - path: "src/components/AIAssistant.js"
      provides: "Animated neural laser scanning line overlay"
      min_lines: 100
    - path: "src/screens/NotesScreen.js"
      provides: "X-mark square checklist boxes in renderMarkdown"
      min_lines: 100
  key_links: []

---

<objective>
Refactor screen elements, mechanical square buttons, X-mark square checkboxes, and AI laser scanning overlays to achieve a complete high-fidelity industrial console experience.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10/10-RESEARCH.md
@/UI Design plan/kwestup/DESIGN.md
@src/components/CustomButton.js
@src/components/AIAssistant.js
@src/screens/NotesScreen.js
@src/components/TaskCard.js
</context>

<interfaces>
DESIGN.md definitions:
- Buttons: The 'Primary' button is pure white with black text; 'Secondary' is black with a white 2px border. On press, use an 'Invert' effect.
- Checkboxes: Square boxes. When checked, they are filled with a solid 'X' mark.
- AI Elements: Glassmorphic containers with a backdrop blur and 10% white tint, plus a 1px horizontal white scanning line that moves vertically.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Redesign CustomButton as mechanical switch button</name>
  <files>
    - src/components/CustomButton.js
  </files>
  <read_first>
    - src/components/CustomButton.js
  </read_first>
  <action>
    Modify `src/components/CustomButton.js`:
    1. Set `borderRadius: 0` to achieve perfect rectangle corners.
    2. Add standard raised outward bevel borders:
       - `borderWidth: 2`, `borderColor: '#000000'`.
    3. Implement mechanical state inversion:
       - Wrap the button in a standard `<Pressable>` component.
       - Use a dynamic function for children/styling: `({ pressed }) => [...]`.
       - If button is primary:
         - Standard: background `white`, text `black`.
         - Pressed: background `black`, text `white`, and trigger light haptic bump (`Haptics.impactAsync`).
       - If button is secondary:
         - Standard: background `black`, text `white`, border white.
         - Pressed: background `white`, text `black`, border black.
  </action>
  <verify>
    <automated>
      grep -q "Pressable" src/components/CustomButton.js && echo "✓ CustomButton rebuilt using dynamic Pressable states"
      grep -q "borderRadius: 0" src/components/CustomButton.js && echo "✓ CustomButton corners are perfectly sharp"
    </automated>
  </verify>
  <acceptance_criteria>
    - CustomButton has sharp corners and custom raised borders.
    - Button triggers full color-inversion on press to mimic mechanical hardware.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build X-mark square checkboxes in Notes and Tasks</name>
  <files>
    - src/screens/NotesScreen.js
    - src/components/TaskCard.js
  </files>
  <read_first>
    - src/screens/NotesScreen.js lines 356-381 (checkbox rendering)
    - src/components/TaskCard.js (task list checkbox)
  </read_first>
  <action>
    1. Update `renderMarkdown` inside `src/screens/NotesScreen.js`:
       - Replace checked checkbox icon (`checkbox-marked`) with a square outline containing a bold **X** (using the `close` Material icon in white on black background, or styling custom square borders with inset lines).
       - Ensure unchecked checkbox renders a perfect square border (`width: 20`, `height: 20`, `borderWidth: 2`, `borderColor: outlineColor`, `borderRadius: 0`).
    2. Update checkbox inside `src/components/TaskCard.js` to render the exact same X-mark square design for checked tasks.
  </action>
  <verify>
    <automated>
      grep -q "close" src/screens/NotesScreen.js && echo "✓ Checklist now renders sharp mechanical X marks"
      grep -q "close" src/components/TaskCard.js && echo "✓ Tasks now render square X mark checkboxes"
    </automated>
  </verify>
  <acceptance_criteria>
    - Notes and Tasks utilize square outline check-boxes that display a solid "X" instead of a checkmark.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rebuild AIAssistant.js featured card with looping laser scan animation</name>
  <files>
    - src/components/AIAssistant.js
  </files>
  <read_first>
    - src/components/AIAssistant.js
  </read_first>
  <action>
    Modify `AIAssistant.js`:
    1. Setup React Native Animated value: `const scanAnim = useRef(new Animated.Value(0)).current;`.
    2. Create a looping loop inside `useEffect` that updates `scanAnim` from `0` to `1` every 3 seconds:
       ```javascript
       Animated.loop(
         Animated.sequence([
           Animated.timing(scanAnim, {
             toValue: 1,
             duration: 3000,
             useNativeDriver: false,
           })
         ])
       ).start();
       ```
    3. Render an absolute-positioned scan line container inside the AI featured glass card:
       ```javascript
       const laserY = scanAnim.interpolate({
         inputRange: [0, 1],
         outputRange: ["0%", "100%"]
       });
       ```
    4. Style the scan line:
       - `position: 'absolute'`, `left: 0`, `right: 0`, `height: 2`, `backgroundColor: '#FFFFFF'`, `opacity: 0.8`.
       - Render a neon laser reflection: `boxShadow: '0 0 8px 1px #FFFFFF'`.
  </action>
  <verify>
    <automated>
      grep -q "scanAnim" src/components/AIAssistant.js && echo "✓ AIAssistant laser scan animation successfully constructed"
    </automated>
  </verify>
  <acceptance_criteria>
    - AIAssistant featured banner renders a sliding horizontal laser scan line.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
No threat boundary changes.
</threat_model>

<verification>
Verify eslint returns 0 errors on modified files:
```bash
npx eslint src/components/CustomButton.js src/components/AIAssistant.js src/screens/NotesScreen.js src/components/TaskCard.js
```
</verification>

<success_criteria>
- CustomButton, checkboxes, and AI animations render perfectly with zero linter errors.
</success_criteria>

<output>
After completion, create `.planning/phases/10/10-02-SUMMARY.md`
</output>
