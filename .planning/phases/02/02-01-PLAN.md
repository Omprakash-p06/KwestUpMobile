---
phase: 02-tasks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [App.js, AppNavigator.js, TaskListScreen.js]
autonomous: true
requirements: [TASK-01, TASK-02]
must_haves:
  truths:
    - "Task lists state is initialized in App.js with a default category seeded"
    - "New lists are loaded, saved, and reset cleanly inside AsyncStorage cycles"
    - "Top tab navigation horizontal bar renders categories on screen"
  artifacts:
    - path: "App.js"
      provides: "Relational task lists state, AsyncStorage load/save cycles, and CRUD handlers"
    - path: "src/screens/TaskListScreen.js"
      provides: "Top navigation horizontal tabs bar"
---

<objective>
Establish the relational lists data structures, AsyncStorage caching lifecycles, and top navigation tab controllers for Phase 2.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Task Lists State in App.js</name>
  <files>App.js</files>
  <action>
    - Add `taskLists` state initialized with default `"My Tasks"` category.
    - Setup `loadData`, `saveData`, and `handleResetData` to manage list arrays.
  </action>
  <acceptance_criteria>
    - App.js contains the `taskLists` state hook.
    - Default `default_inbox` list is seeded on startup if empty.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Lists state is correctly initialized and serialized in AsyncStorage.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Lists CRUD Handlers</name>
  <files>App.js</files>
  <action>
    - Add custom list helpers: `handleCreateList`, `handleRenameList`, `handleDeleteList`.
    - Configure cascading deletions to cleanly cancel alerts for nested tasks.
  </action>
  <acceptance_criteria>
    - App.js contains list CRUD helper functions.
    - Delete lists triggers safety warnings and purges records.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>CRUD handlers are ready for lists management.</done>
</task>

<task type="auto">
  <name>Task 3: Propagate Props through AppNavigator</name>
  <files>src/navigation/AppNavigator.js</files>
  <action>
    - Forward `taskLists` and custom CRUD hooks to the Task screen container.
  </action>
  <acceptance_criteria>
    - AppNavigator includes `taskLists` inside its parameter block and forwards it to `TaskListScreen`.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Properties are successfully delegated.</done>
</task>

<task type="auto">
  <name>Task 4: Build Top Category Tabs scrollbar</name>
  <files>src/screens/TaskListScreen.js</files>
  <action>
    - Add a horizontal scrollbar of tab category buttons at the top of the Task screen.
  </action>
  <acceptance_criteria>
    - TaskListScreen displays list buttons that snap active selections.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Tabs navigation renders beautifully.</done>
</task>

</tasks>
