---
phase: 03-birthdays
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [App.js, src/screens/BirthdaysScreen.js, src/components/TaskCard.js]
autonomous: true
requirements: [REMD-01]
must_haves:
  truths:
    - "Birthdays state is initialized to track full birthdate dates"
    - "Birthday list dynamically sorts by closest upcoming countdown"
    - "Celebrations trigger confetti blasts and success haptic indicators"
  artifacts:
    - path: "App.js"
      provides: "Global state loading and saving lifecycles for birthdays"
    - path: "src/screens/BirthdaysScreen.js"
      provides: "Confetti cannon celebrate triggers and closest dynamic sorting"
---

<objective>
Deliver the core birthday data structure, countdown calculation math, closest list sorting, and feedback celebrators.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Setup Birthday state schema in App.js</name>
  <files>App.js</files>
  <action>
    - Add birthdays state and setup JSON load/save serializers.
  </action>
  <acceptance_criteria>
    - App.js loads and serializes birthday array schema fields.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Birthdays array schema is fully integrated into AsyncStorage.</done>
</task>

<task type="auto">
  <name>Task 2: Dynamic closest upcoming sorting</name>
  <files>src/screens/BirthdaysScreen.js</files>
  <action>
    - Calculate days remaining for each contact.
    - Sort the list dynamically by closest upcoming dates.
  </action>
  <acceptance_criteria>
    - Birthdays screen dynamically displays closest countdown first.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Upcoming countdown sorting works perfectly.</done>
</task>

<task type="auto">
  <name>Task 3: Haptics & Confetti celebrations</name>
  <files>src/screens/BirthdaysScreen.js</files>
  <action>
    - Trigger native success haptic click on birthday addition.
    - Trigger confetti cannon blasts and haptics on card press celebration.
  </action>
  <acceptance_criteria>
    - Birthday card press triggers confetti blasts on viewport.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Celebrate indicators are active.</done>
</task>

</tasks>
